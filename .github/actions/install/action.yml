name: 'Install Dependencies'
description: 'Install pnpm dependencies with frozen lockfile'

runs:
  using: 'composite'
  steps:
    - name: Install
      shell: bash
      run: pnpm install --frozen-lockfile
